# Sub2Balance 使用手册

面向实际操作人员的功能说明。部署相关请看 [DEPLOYMENT.md](DEPLOYMENT.md)。

---

## 一、角色分工

系统内有两种角色：

| 角色 | 能做什么 |
| --- | --- |
| **Staff（员工）** | 查询客户订阅、提交转换申请、查看自己提交过的申请 |
| **Admin（管理员）** | 上述全部 + 审批申请 + 管理员工账号 + 配置套餐价格 + 配置 Sub2API + 查看所有操作日志 |

首次登录使用的是 `.env` 里配的 `ADMIN_EMAIL` / `ADMIN_PASSWORD`（第一个 admin）。之后所有账号由 admin 在「用户管理」里创建。

---

## 二、登录与修改密码

### 登录
打开 `http://<部署地址>:8080`，输入邮箱密码。根据角色自动跳转：

- Admin → 管理员控制台 `/admin`
- Staff → 创建申请页 `/requests/new`

### 修改密码
任何时候，点右上角用户邮箱旁边的 **修改密码** 按钮。修改后需要重新登录。

### 退出登录
同样在右上角，点 **退出登录**。

---

## 三、员工（Staff）使用流程

### 1. 创建转换申请

菜单 → **创建申请**（路径 `/requests/new`）。分三步：

#### Step 1: 查询客户订阅

在顶部输入框填客户邮箱，点 **查询**。系统从 Sub2API 拉取该邮箱下的所有订阅。

- 如果查不到 → 说明客户没有订阅，或邮箱拼错
- 返回的每条订阅是一张卡片，显示：套餐名、状态、到期时间、金额等

#### Step 2: 选中要操作的订阅

**只有 `active`（有效）状态的订阅才能操作。** 点卡片，选中的会变成亮蓝色边框。

#### Step 3: 选择转换方式

两个按钮切换：

##### 方式 A：转按量（balance）

把订阅的未消费额度转成账户余额。显示三项：

- **原始金额**：套餐全额
- **已消费**：截至目前已经使用掉的
- **可转换**：`max(0, 原始金额 − 已消费)`

默认就是把「可转换」的金额给客户。如果你想自定义（比如扣手续费、或按协商金额），点 **覆盖购买金额**，输入自定义数字。要恢复默认，点 **恢复默认** 按钮。

公式展示区会告诉你最终成交金额。

提交前确认：**可转换金额 > 0** 才能提交，否则按钮是禁用状态。

##### 方式 B：切换套餐（switch）

从 A 套餐切换到 B 套餐，指定新套餐的有效期。

- **目标套餐**：下拉菜单里选（只列出可切换的套餐）
- **有效期**：输入天数，必须 > 0
- 预览行会显示：`A套餐 → B套餐 · N天`

⚠️ **注意**：A 套餐未消费的额度**作废**，不转成余额。如果客户想保留余额，用方式 A。

### 2. 提交后

填完点 **提交申请**，跳转到 **我的申请**。申请进入 `pending`（待审核）状态，等 admin 审批。

### 3. 查看我的申请

菜单 → **我的申请**（路径 `/requests`）。表格列：编号、类型、客户邮箱、分组、详情、状态、提交时间。

状态有三种：
- `pending` 待审核 — 黄色
- `approved` 已通过 — 绿色
- `rejected` 已拒绝 — 红色

点 **查看** 打开详情弹窗，能看到审批备注（如果 admin 填了的话）和审批时间。

---

## 四、管理员（Admin）工作流

登录后首先到 **控制台**（`/admin`），顶部有四个数据卡片：待审核数、已通过、已拒绝、用户总数。下面是六个快捷入口。

### 1. 审批申请

菜单 → **待审批**（路径 `/admin/pending`）。表格列出所有 `pending` 申请，每行右侧有 **审核** 按钮。

点 **审核** 打开弹窗。根据申请类型内容不同：

#### Balance 类型（转按量）
显示：原始金额 / 已消费 / 可转换 + 员工填的金额覆盖（如有）。

可以在审批弹窗里**二次修改最终金额**（`final_amount`）。留空就用员工提交的金额；填了就用你填的。

#### Switch 类型（切换套餐）
显示：当前套餐 / 目标套餐 / 有效期天数。没有金额字段，不能改。

#### 通用
- **审批备注**：通过时可填可不填；**拒绝时必填**（没填点拒绝会弹红字 toast 提醒）
- 两个按钮：**通过** 或 **拒绝**

#### 点击通过之后会发生什么

系统自动调用 Sub2API：
- Balance：取消订阅 + 给客户账户加余额
- Switch：取消当前订阅 + 创建目标套餐订阅

如果 Sub2API 报错，申请状态不会变成 approved，错误会 toast 给你。去 **操作日志** 看详细错误信息。

### 2. 全部申请

菜单 → **全部申请**（`/admin/requests`）。比待审批多了：

- 可以看到所有状态的申请，不只是 pending
- 多了「提交人邮箱」一列，能看出是哪个员工提交的
- 右上角 **状态筛选**：全部 / 待审核 / 已通过 / 已拒绝

### 3. 用户管理

菜单 → **用户管理**（`/admin/users`）。

**新建员工账号：**
点 **新增用户** → 填邮箱 + 密码（≥ 8 位）+ 选角色（staff/admin）→ 提交。

**删除：**
表格每行右侧 **删除** 按钮，有二次确认弹窗。⚠️ 删除的是账号，不是该员工历史提交的申请 —— 申请记录会保留，提交人字段显示为原邮箱。

### 4. 套餐价格映射

菜单 → **套餐价格**（`/admin/group-prices`）。

**背景：** 计算「可转换金额」时，价格来源有三个，优先级从高到低：
1. **手动映射价格**（这个页面配的）
2. **从套餐名解析**（系统根据套餐名关键字推断的）
3. **USD 日限额**（Sub2API 返回的默认额度）

表格列：套餐 ID、名称、平台（Claude Code / Codex）、日限额（USD）、解析价格（灰字，自动推断值）、映射价格（绿字，你配的值）、操作。

**绑定 / 编辑：**
- 未映射 → 点 **绑定** → 填实收金额 + 币种（CNY / USD）+ 备注 → 保存
- 已映射 → **编辑** 改值，**清除** 删掉映射（回落到解析价格）

**什么时候需要配？**
- 解析出来的价格和你实际收款金额对不上时（最常见）
- 推出了新套餐，系统还没学会识别时
- 老板临时调了某套餐价格时

### 5. Sub2API 配置

菜单 → **系统设置**（`/admin/settings`）。页面顶部有状态指示：

- 绿色 **已配置** → 系统能正常工作
- 琥珀色 **未配置** → 所有查询 / 转换都会失败

**字段：**
- **Base URL**：Sub2API 实例地址（必填）
- **API Key**：有 admin 权限的 key
  - 首次配置必填
  - 已配置过后，这里会显示为掩码（类似 `sk-***`），留空就是保留不改，填了就是覆盖

**两个按钮：**
- **测试连接**：实际打一次 API 探测，成功 / 失败 toast
- **保存**：把配置写入数据库

重启服务不会丢配置 —— 这些凭据存在 `data/sub2balance.db` 里，不在 config.yaml。

### 6. 操作日志

菜单 → **操作日志**（`/admin/audit-logs`）。所有关键动作都有记录：登录、提交申请、审批、用户管理、配置改动等。

表格列：编号、操作人邮箱、操作类型、关联申请 ID、详情、时间。每页 50 条，底部翻页。

**典型用途：**
- 查某个失败申请的具体错误信息（详情字段）
- 追溯某员工 / 某天的操作
- 合规审计

---

## 五、常见错误 & 解释

| 看到的提示 | 含义 / 处理 |
| --- | --- |
| `sub2api is not configured` | Admin 没配 Sub2API。去 `/admin/settings` 配。 |
| 登录报「邮箱或密码错误」 | 核对 `.env` 里的 `ADMIN_EMAIL` / `ADMIN_PASSWORD`；注意第一次启动后修改 `.env` 不会生效，密码已进 DB。 |
| 查询订阅报「上游请求失败」 | 多半 Sub2API 挂了或 Base URL / API Key 错了。先去「系统设置」点 **测试连接**。 |
| 审批时「通过」按钮点了没反应 | 看 toast；如果一闪而过，开浏览器 DevTools Network 看 `/api/admin/conversions/:id/approve` 响应。 |
| 员工说提交按钮是灰的 | `active` 订阅才能操作；balance 模式下「可转换」= 0 也会禁用提交。 |
| 拒绝申请弹「备注必填」 | 拒绝必须写理由，是有意的（审计合规 + 让员工知道为啥驳回）。 |
| 切换套餐后客户说「我的余额没到」 | 切换模式不转余额，是**作废**未消费额度。要转余额选 balance 模式。 |
| 金额对不上 | 很可能该套餐需要手动映射价格。去 `/admin/group-prices` 配。 |

---

## 六、完整申请生命周期

```
Staff                          Admin                         Sub2API
 │                               │                              │
 │ 1. 输邮箱查订阅                │                              │
 │ ─────────────────────────────────────────────────────────────>│
 │                                                              │
 │<──────────────── 订阅列表 ─────────────────────────────────────│
 │                                                              │
 │ 2. 选订阅 + 选方式 + 提交      │                              │
 │ ──────> DB (status=pending)   │                              │
 │                               │                              │
 │                               │ 3. 在「待审批」看到           │
 │                               │                              │
 │                               │ 4. 打开审批弹窗 + 通过        │
 │                               │ ─────────────────────────────>│
 │                               │                              │ 取消订阅
 │                               │                              │ 加余额 或
 │                               │                              │ 切套餐
 │                               │<─────────── 成功/失败 ────────│
 │                               │                              │
 │                               │ 5. DB (status=approved)      │
 │                               │    + 写 audit log            │
 │                                                              │
 │ 6. 「我的申请」看到 approved   │                              │
```

失败路径：第 4 步 Sub2API 返回错误，status 保持 `pending`，audit log 记录失败原因，admin 看 toast。

---

## 七、常见场景 Cookbook

### 「客户想退订阅，拿回未用完的金额」
Staff → 创建申请 → 查邮箱 → 选该订阅 → **转按量** → 不改默认金额 → 提交 → Admin 审批通过。

### 「客户想从 A 套餐换成 B 套餐，不要余额」
Staff → 查邮箱 → 选 A 订阅 → **切换套餐** → 选 B 套餐 + 填天数 → 提交 → Admin 审批通过。A 的未消费部分作废。

### 「协商过只退一半」
Staff → 走「转按量」流程 → 勾选 **覆盖购买金额** → 填实际协商金额 → 提交。Admin 审批前还能在审批弹窗里二次调整（填 `final_amount`）。

### 「新套餐上线，价格识别错了」
Admin → **套餐价格** → 找到该套餐 → **绑定** → 填对应金额 + 币种 → 保存。之后所有新提交都按这个价。

### 「员工要休假，临时停掉账号」
Admin → **用户管理** → **删除**。想保留账号只是禁用登录 —— 目前没这个功能，只能删。（如果需要可以提给开发加。）

### 「排查『上次那单 Sub2API 明明出钱了但状态还是 pending』」
Admin → **操作日志** → 按时间或申请 ID 过滤（目前只能翻页找）→ 看「详情」字段的错误信息 → 确认是 Sub2API 哪一步失败 → 必要时联系 Sub2API 管理员 → 手工修复后可以通过 DB 直接改状态（或重新提交）。

---

## 八、限制与已知边界

- **暂无** 批量操作（批量审批、批量导出）
- **暂无** 用户启用 / 禁用（只能删除）
- **暂无** 按客户邮箱搜索历史申请 —— 需要翻「全部申请」或查 DB
- **暂无** 申请撤回 —— pending 状态也只能由 admin 驳回
- **审计日志**只能按分页浏览，无搜索 / 过滤 UI

这些需求如果业务上急需，联系研发加。
